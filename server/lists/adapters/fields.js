// const fieldsAdapter = async (data, { logger }) => {
//     logger.debug('REQUEST_FIELDS', { fields: data.length });
//     return data.map(({ label, value }) => ({ key: value, value: label }));
// };
//
// module.exports = {
//     fieldsAdapter
// }
